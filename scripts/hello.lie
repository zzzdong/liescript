use std::fmt;

// DATA
struct Data {
    count: int,
    score: float,
}


fn hello() {
    std::fmt::println("Hello, world!");
}


fn fib(n: int) -> int {
    if n == 0 {
        0
    } else {
        if n == 1 {
            1
        } else {
            fib(n - 1) + fib(n - 2)
        }
    }
}

fn main() {
    hello();
}
